export const getNameInitial = (firstName, lastName) => {
    return firstName.toUpperCase()[0] + lastName.toUpperCase()[0]
}

// export const getNameInitial = (firstName, lastName) => {
//     return firstName.toUpperCase()[0] + firstName.toLpperCase()[0]
// }




  